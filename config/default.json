{
  "port": "env(PORT)",
  "settings": {
    "loggerFormat": "tiny",
    "templateEngine": "twig",
    "session": {
      "store": "@foal/typeorm"
    }
  },
  "database": {
    "type": "better-sqlite3",
    "database": "./db.sqlite3"
  }
}
